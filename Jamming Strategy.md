#### A. Reactive Jamming Strategy

在本系统中，我们考虑一个智能的感知-反应型干扰机。与持续广播干扰噪声的常规干扰机不同，该干扰机采用“感知-检测-攻击”的动态闭环策略，仅在检测到目标用户的活跃传输时才发射干扰信号。这种策略不仅极大地降低了干扰机的功耗，还显著增加了被反干扰系统检测和规避的难度。

干扰机的行为被建模为一个连续的时间状态机，主要包含两个核心阶段：频谱感知与干扰攻击。干扰机维护两个关键的时间参数：感知窗口时长 $\tau_{\text{sense}}$ 和干扰持续时长 $\tau_{\text{jam}}$。

**1) 频谱感知与能量检测:**
由于干扰机无法预知发送方的跳频序列，它采用基于预设扫描图案的搜索策略。在任意时刻 $t$，干扰机驻留在扫描序列指定的第 $k$ 个信道上，采集时长为 $\tau_{\text{sense}}$ 的基带复信号样本 $r_J(n)$。设系统采样率为 $f_s$，则感知窗口内的样本数为 $N = \lfloor \tau_{\text{sense}} f_s \rfloor$。

干扰机采用能量检测器来判断当前信道是否存在合法传输。这一过程被建模为如下二元假设检验问题：
$$
\begin{cases}
H_0:\; r_J(n)=w(n), \quad \text{信道空闲}\\
H_1:\; r_J(n)=h_{TI}s(n)+w(n), \quad \text{信道被占用}
\end{cases}
$$
其中，$s(n)$ 表示目标用户的发射信号，$h_{TJ}$ 表示发射机到干扰机的信道增益，$w(n)$ 为服从零均值复高斯分布的加性白噪声，其方差为 $\sigma_w^2$。

干扰机计算接收信号在感知窗口内的能量统计量 $E$：
$$
E = \sum_{n=0}^{N-1} |r_J(n)|^2
$$
根据统计信号处理理论，在 $H_0$ 假设下，归一化能量统计量 $\frac{2E}{\sigma_w^2}$ 服从自由度为 $2N$ 的中心卡方分布。在 $H_1$ 假设下，该统计量服从非中心卡方分布，其非中心参数 $\lambda$ 取决于接收到的瞬时信噪比。

为了严格控制虚警率 $P_{FA}$，干扰机依据预设的目标虚警率 $\alpha$ 设定检测阈值 $\eta$。该阈值由下式解析确定：
$$
\eta = \frac{\sigma_w^2}{2} \cdot F_{\chi^2_{2N}}^{-1}(1 - \alpha)
$$
其中 $F_{\chi^2_{2N}}^{-1}(\cdot)$ 表示自由度为 $2N$ 的卡方分布的逆累积分布函数。

**2) 干扰决策与信号生成:**
在感知窗口结束时，干扰机将计算出的统计量 $E$ 与阈值 $\eta$ 进行比较，并根据结果执行相应的动作：

- **若 $E < \eta$ ：** 干扰机推断当前信道空闲。它将立即切换至预设扫描图案中的下一个逻辑信道，并重新进入感知状态，开始新一轮的频谱监测。
- **若 $E \geq \eta$ ：** 干扰机判定当前信道存在活跃传输，随即触发攻击模式。

在攻击模式下，干扰机锁定当前信道中心频率 $f_c$，并立即发射高功率干扰信号。为了最大化对相位调制信号星座图的破坏效果，本模型采用**单音干扰**策略。生成的干扰波形 $J(t)$ 表示为：
$$
J(t) = \sqrt{2 P_J} \cos(2\pi f_c t + \phi_J)
$$
其中 $P_J$ 是干扰发射功率，$\phi_J$ 是在 $[0, 2\pi)$ 范围内服从均匀分布的随机相位。单音干扰能有效地使接收端的解调星座点发生偏移和混叠，从而导致严重的误符号率。

干扰信号持续发射时长为 $\tau_{\text{jam}}$。攻击结束后，干扰机停止发射并立即返回频谱感知状态，继续搜寻目标信号。该策略使得干扰机能够以极低的时延自适应地跟踪并阻断目标的通信链路。